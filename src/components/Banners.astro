---
import BannerCarousel from '../components/react/BannerCarousel'


import { db } from '../db/db';

const banners = await db.query.banners.findMany({
  orderBy: (banners, { asc }) => [asc(banners.id)],
});
---


<BannerCarousel client:load banners={banners} />